# 🤖 Урок 4: Панель управления роботом (анимация, события, логика)

## 🎯 Что мы изучим на этом уроке

1. Как получать данные от пользователя (слайдеры, кнопки)
2. Как менять содержимое страницы без перезагрузки (JavaScript + DOM)
3. Что такое события (`click`, `input`) и как на них реагировать
4. Как сделать простую симуляцию управления сервоприводами
5. Как вести лог действий

---

## 🧠 Что такое событие (event)

Событие — это действие пользователя:
- нажатие на кнопку (`click`)
- движение ползунка (`input`)
- наведение мыши (`mouseenter`)

JavaScript может **следить** за событиями и выполнять код в ответ.

### Пример (простой)
```js
button.onclick = function() {
    alert('Кнопка нажата!')
}
```


🎛️ Что такое слайдер (range input)
В HTML есть специальный элемент для ползунков:

<input type="range" min="0" max="180" value="90">

min — минимальное значение

max — максимальное

value — текущее

Зачем это робототехнику?

Каждый палец механической руки может поворачиваться от 0° до 180°.
